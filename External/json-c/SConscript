Import("outerEnv")

env = outerEnv.Clone(CC="clang", CCFLAGS="--std=c99 -O4 -Wall -Werror")
#env.AppendUnique(FRAMEWORKS=["OpenCL"])

libjsonc = env.SharedLibrary("jsonc", Glob("*.c"))

Return("libjsonc")